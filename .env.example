# Optional. Without it, TrailCast falls back to its deterministic
# parser + template narrator and every feature still works.
ANTHROPIC_API_KEY=

# Optional override for the Anthropic model used by the ask layer.
ANTHROPIC_MODEL=claude-sonnet-4-5
